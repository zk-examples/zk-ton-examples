
#include "imports/stdlib.fc";

;; ops
const op::verify = "op::verify"c;

;; input constants (IC0 is the base term)
const slice verifier::IC0 = "a6bae9f02dcf85c4a5caa934276b406d3029faab6b8281a870f3ffe4a5965991cdfc3fbb999f4e62ee4c16ffecca2cea"s;
const slice verifier::IC1 = "abefddb84f28bc8e7d5be1098d3850905f05b3b25bcbae082242ffc1bffacc465442187680358ddaebca88a2fdd8921b"s;

;; vk constants
const slice verifier::vk_gamma_2 = "b21dc4e3c36392a7d5fafb4177dd209416851bb1a008b196d18e6bd42581e9efe1944ef655ec6c87bb1b4520c347e37d0392b3a0eeef8f9c2c5893dab6acd5a91b3076655aa57581b2c417e9df36e7c183ecf2b450ddff3f7138699ff14a7d6f"s;
const slice verifier::vk_delta_2 = "a55004e96f07b7b0bef71dad062ae868093e1771d4b65e47c42dc0abf7365ef228d66d1bf1dea0cc32d6d86b96270d8f0b005fef4fecbe3de8699f33995e6a3d5184b29a568451110895d583d5fe142b634d9e651c735ab49459830d08c2d579"s;
const slice verifier::vk_alpha_1 = "a89bcc7dd74bd49ad2b36e6074d1cf55007fd2142b40b1e72c2047616e1ee1f1b6ad34700126626698f03e037edd60c9"s;
const slice verifier::vk_beta_2  = "87d17b4de58e0fbccb52970e229a21b475ccc8db384b6dfa02ef4d1fdf98c08c0905f80e24b21c188ac23cd90624e1d80b1bfe1d2ce4541452a96e2b3108c8907605050151d966c5302364629c02708134dbc7ce8d7e587fd46cd5e8e58b77c7"s;

;; public input dict settings
const int verifier::publicInputKeyLen = 32;
const int verifier::nPublic = 1;

;; error codes
const int verifier::err::public_not_present = 257;
const int verifier::err::too_many_publics = 258;
const int verifier::err::wrong_proof = 260;

;; bls intrinsics
slice bls_g1_add(slice x, slice y) asm "BLS_G1_ADD";
slice bls_g1_neg(slice x) asm "BLS_G1_NEG";
int bls_pairing(slice x1, slice y1, slice x2, slice y2, slice x3, slice y3, slice x4, slice y4, int n) asm "BLS_PAIRING";

slice bls_g1_multiexp_1(
    slice x1, int y1
    , int n
) asm "BLS_G1_MULTIEXP";

(int) groth16Verify(
    slice piA,
    slice piB,
    slice piC,
    cell pubInputs
) impure {

    ;; extract public inputs by keys 0..nPublic-1
    (slice input0, int ok0) = pubInputs~udict_delete_get?(verifier::publicInputKeyLen, 0);
    throw_unless(verifier::err::public_not_present, ok0);
    int val0 = input0~load_uint(256);

    ;; ensure the dict is now empty (no extra public inputs beyond 0..nPublic-1)
    throw_unless(verifier::err::too_many_publics, null?(pubInputs));

    ;; assemble cpub via batched MULTIEXP and summation
    slice cpub = bls_g1_multiexp_1(
        verifier::IC1, val0
        , 1
    );

    ;; add the free term IC0
    cpub = bls_g1_add(cpub, verifier::IC0);

    ;; pairing check
    slice piA_neg = bls_g1_neg(piA);
    int a = bls_pairing(
        cpub,              verifier::vk_gamma_2,
        piA_neg,          piB,
        piC,              verifier::vk_delta_2,
        verifier::vk_alpha_1, verifier::vk_beta_2,
        4
    );

    return (0 - (a != 0));
}

(int) verifyProof(
    slice in_msg_body
) impure {
    slice piA = in_msg_body~load_ref().begin_parse();
    slice piB = in_msg_body~load_ref().begin_parse();
    slice piC = in_msg_body~load_ref().begin_parse();
    var pubInputs = in_msg_body~load_dict();

    return groth16Verify(
        piA,
        piB,
        piC,
        pubInputs
    );
}

() recv_internal(cell in_msg_full, slice in_msg_body) impure {
    if (in_msg_body.slice_empty?()) {
        return ();
    }
    slice cs = in_msg_full.begin_parse();
    int flags = cs~load_uint(4);
    if (flags & 1) {
        return ();
    }
    int op = in_msg_body~load_uint(32);

    if (op == op::verify) {
        throw_unless(verifier::err::wrong_proof, verifyProof(in_msg_body));
        return ();
    }
    throw(0xffff);
}

int verify(slice piA, slice piB, slice piC, slice pubInputs) method_id {
  return groth16Verify(piA, piB, piC, pubInputs~load_dict());
}
